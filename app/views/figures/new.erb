<h1>Create a New Figure:</h1>
   <form action="/figures" method="POST">
     <h2>Figure Name:</h2>
     <input type="text" name="figure[name]" id="figure_name">
       <h3>Choose an existing title:
       <% Title.all.each do |title| %>
         <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%=title.id%>"><%=title.name%></h3>
       <%end%>
       <h3>Or create a new title:<input type="text" name="title[name]" id="new_title"></h3>

       <h3>Choose an existing landmark:</h3>
       <%Landmark.all.each do |landmark|%>
         <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><%=landmark.name%></input>
       <%end%>
       <h3>Or create a new landmark:<input type="text" name="landmark[name]" id="new_landmark"></h3>

       <input type="submit" name="submit" id="Create New Figure">
  </form>
